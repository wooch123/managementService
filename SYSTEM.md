# SYSTEM.md — 시스템 구성과 작업 정리

> 이 문서는 **지금 무엇이 어떻게 돌아가고 있는지**를 한 장에 모은 것이다.
> 요구사항 명세는 [SPEC.md](SPEC.md), 작업 규칙은 [CLAUDE.md](CLAUDE.md), 시간순 기록은 [PROGRESS.md](PROGRESS.md)에 있다.
> 최종 갱신: 2026-08-19

---

## 1. 한 줄 요약

관리자가 **화면·컴포넌트·DB·동작을 GUI로 설계**하면 검증을 거쳐 운영 사이트(`/home`)에 배포되는 노코드 웹 애플리케이션 빌더이고,
그 빌더 위에 **반도체 스토리지(eMMC/UFS) Claim 통합 분석 서비스**가 올라가 실제로 서비스되고 있다.

| 항목 | 현재 값 |
|---|---|
| 공개 주소 | https://demo.dove9999.com (Cloudflare Tunnel → 로컬 pm2) |
| 배포 리비전 | **#32** |
| 페이지 / 배치된 컴포넌트 | 16개 / 157개 |
| 엔티티 / 액션 / 관계 | 8개 / 21개 / 88개 |
| 운영 데이터 | app.db 20,324행(인덱스 37개) + 게시판 2,001글 |
| 컴포넌트 카탈로그 | 89종 |
| 자동 검사 | 단위 282건 · E2E 10건 · 카탈로그 렌더 89종 · UI 배치 17화면×2해상도 |

---

## 2. 시스템 구성

### 2.1 설계 → 배포 → 운영의 흐름

```
[관리자 화면 /admin]                      [운영 화면 /home]
 builder  graph  data  validate  deploy         │
      │      │     │       │        │           │
      └──────┴─────┴───────┴────────┘           │
                   ↓ 초안(draft)                 │
            ┌──────────────┐                     │
            │  meta.db     │  설계 메타데이터     │
            │ (Prisma)     │  + 플랫폼 기능 데이터 │
            └──────────────┘                     │
                   ↓ 검증 51규칙                  │
                   ↓ 배포(8단계 트랜잭션)          │
            ┌──────────────┐                     │
            │ Revision     │ ← 불변 스냅샷(specJson)
            │ specJson     │────────────────────▶│ 활성 리비전만 렌더
            └──────────────┘                     │
                   ↓ 동적 DDL                     ↓ 바인딩 조회
            ┌──────────────┐            ┌──────────────────┐
            │  app.db      │◀───────────│ data-engine      │
            │ (운영 데이터) │            │ (식별자 검증+바인딩)│
            └──────────────┘            └──────────────────┘
```

- **초안과 운영이 분리된다.** 관리자가 편집하는 것은 초안이고, 운영 화면은 *배포된 리비전*만 본다.
- **리비전은 불변이다.** 배포할 때 설계 전체를 `specJson`으로 굳혀 저장하므로, 이후 초안을 고쳐도 운영은 흔들리지 않는다.
- 배포는 검증 → 스키마 변경 → 스냅샷 → 활성 리비전 교체까지 한 트랜잭션으로 처리한다.

### 2.2 저장소 3종

| 파일 | 담당 | 접근 방법 |
|---|---|---|
| `prisma/meta.db` | 설계 메타데이터(페이지·컴포넌트·엔티티·액션·관계·리비전) + **플랫폼 기능 데이터**(게시판 글, 채팅, 앱 설정, 관계도 좌표) | Prisma. 게시판 **검색만** 읽기 전용 드라이버 연결(FTS 성능 때문, §4.3) |
| `data/app.db` | 관리자가 설계한 업무 테이블(동적 DDL) | 반드시 `src/lib/data-engine` 경유. 식별자 화이트리스트 + 파라미터 바인딩 |
| `data/backups/` | 두 DB의 일별 백업(30일 보관) | `pnpm db:backup` |

### 2.3 런타임 · 배포 구성

```
Cloudflare Tunnel (cloudflared, pm2 fork 1개)
        │  demo.dove9999.com → 127.0.0.1:3000
        ▼
pm2 cluster — webapp-v1 워커 4개 (next start)
        │        환경변수 NEXT_DIST_DIR = .next-a | .next-b
        ├── meta.db (WAL, busy_timeout 5s)
        └── app.db  (WAL, busy_timeout 5s)
```

- **무중단 배포**: 지금 서비스 중이 아닌 폴더에 빌드하고, 다 만든 뒤 프로세스만 그쪽으로 옮긴다(`deploy/redeploy.ps1`).
  헬스체크에 실패하면 이전 빌드로 자동 롤백하고, 배포 끝에 인덱스를 스펙과 맞춘다.
- **자동 기동**: Windows 작업 스케줄러 `WebApp_V1-autohost` — 로그온 30초 후 1회 + 10분마다 감시.
  이미 정상이면 아무것도 하지 않고, 죽어 있으면 되살린다. 로그는 `data/logs/autostart.log`.
- **다중 워커 대응**: 두 DB 모두 WAL + 잠금 대기, 실시간 채팅은 DB를 통해 워커 간 전파(§4.4).

### 2.4 디렉토리 (핵심만)

```
src/
├─ app/(admin)/admin/{builder,graph,data,validate,deploy}   관리자 5화면
├─ app/(public)/home/[[...slug]]                            운영 렌더러
├─ app/api/{admin,runtime,board,chat,auth,health}           API
├─ components/{builder,graph,runtime,shell,ui}              화면 구성요소
└─ lib/
   ├─ registry/      컴포넌트 카탈로그 89종(그룹·속성 스키마·렌더)
   ├─ runtime/       스펙 → React 렌더, 바인딩 조회, 스펙 캐시
   ├─ data-engine/   동적 DDL·쿼리 빌더·식별자 검증
   ├─ validation/    검증 51규칙
   ├─ actions/       액션 실행기(CREATE/UPDATE/DELETE/QUERY/…)
   ├─ theme/         테마 팔레트 20종
   └─ db/            prisma·app-db·board-search·paths
scripts/   점검·유지보수 도구(§6)
deploy/    ecosystem.json · redeploy.ps1 · start-hosting.ps1 · pm2-status.cjs
```

---

## 3. 빌더가 제공하는 것

- **페이지 트리**: 드래그로 순서·계층 변경, 아이콘 피커. 하위 페이지가 있는 상위 항목도 클릭하면 그 페이지로 이동한다.
- **12칼럼 캔버스**: 컴포넌트 본문 어디나 잡아 이동(드래그 중 커서를 따라온다), 서로 영역을 침범하지 않음, 리사이즈, 우측 패널 영역 편집.
- **컴포넌트 카탈로그 89종**: 레이아웃 10 · 입력 17 · 데이터 표시 13(간트·칸반 포함) · 내비게이션 6 · 피드백 10 · 액션 3 · 유틸리티 9(실시간 채팅 · 기간 필터 포함) · 통계 차트 20 · 게시판 1.
- **관계도(React Flow)**: 페이지별 보기 / 전체 구조 보기, 종류별 밴드 자동 배치(가로 16:9 · 세로 9:16), 보기별 좌표 기억, 노드 더블클릭으로 상세 패널.
- **DB 설계**: 엔티티·필드·관계를 GUI로. 배포 시 동적 DDL로 `app.db`에 반영.
- **액션**: CREATE/UPDATE/DELETE/QUERY/NAVIGATE/TOAST/EXPORT_CSV/COMPOSITE, 성공·실패 후속 액션 연결.
- **검증 51규칙** → **배포**: 오류 0이어야 배포되고, 파괴적 스키마 변경은 명시 확인이 필요하다.
- **테마 20종**: 다크·그레이·라이트·메탈릭·모던 각 4종. 사이드바 하단에서 고르고 브라우저에 저장된다.

---

## 4. 이번에 만든 주요 기능

### 4.1 게시판 프리셋 (배치 즉시 동작)

캔버스에 올리는 순간 **목록·조회·글쓰기**가 동작한다. 엔티티 설계나 액션 연결이 필요 없다.

- 글은 메타 DB의 `BoardPost`에 쌓이고, `boardKey`(기본값 = 배치된 노드 id)로 게시판을 구분한다.
- 에디터는 툴바(굵게·기울임·제목·목록·인용·코드·링크) + 미리보기. 본문은 마크다운으로 저장하고
  `src/lib/markdown.ts`가 토큰 트리로만 파싱해 React 엘리먼트로 그린다 — HTML 문자열을 만들지 않으므로
  방문자가 `<script>`를 써도 글자로만 보인다.
- 검색·분류·조회수·페이지 이동 포함, 삭제는 관리자만.

### 4.2 차트 정확도 — 항목별 집계(`group`) 바인딩

차트가 원시 행을 최대 200건만 가져와 화면에서 세고 있었다. 데이터가 늘자 **5,000건 중 200건 표본**이 되어
실제 1,255건인 항목이 58건으로 그려졌다. `GROUP BY`로 DB가 전부 집계하는 모드를 추가하고 큰 테이블을 보는 차트 8개를 전환했다.
결과 봉투를 기존과 같게 맞춰 차트 컴포넌트는 수정 없이 동작한다. 페이지 HTML도 172KB → 125KB로 줄었다.

### 4.3 게시판 전문 검색 (FTS5 · trigram)

- 한국어는 붙여 쓰는 경우가 많아 기본 토크나이저로는 "상세**분석**"의 "분석"을 못 찾는다 → **trigram** 사용.
- trigram은 3글자 미만을 색인하지 않으므로 **2글자 이하 검색어는 LIKE 경로**로 처리한다(하이브리드).
- 같은 질의가 Prisma raw로 21.3ms, 드라이버로 0.32ms였다 → **검색만** 읽기 전용 드라이버 연결(`src/lib/db/board-search.ts`).
  쓰기는 그대로 Prisma가 전담한다.

### 4.4 실시간 채팅 (SSE) + 다중 워커 전파

수신은 SSE, 발신은 REST. 워커가 여러 개면 메모리 전파만으로는 다른 워커에 닿지 않으므로
**DB를 전파 통로로** 쓴다 — 구독자가 있는 동안만 0.7초 주기로 새 메시지를 확인해 자기 워커 구독자에게 밀어준다.
새 미들웨어(Redis 등) 없이 다중 워커를 지원하는 대신, 다른 워커의 메시지는 최대 0.7초 늦게 도착한다.

### 4.5 차트 축 처리

- **x축**: 항목이 많아도 하나도 건너뛰지 않는다. 가로로 안 들어가면 -35°(아주 빽빽하면 -60°)로 기울이고,
  축 높이를 넘길 만큼 긴 이름만 말줄임한다.
- **양 끝 잘림 방지**: 가로쓰기는 눈금 위에 가운데 정렬이라 첫/마지막 레이블의 절반이, 기울인 글자는
  왼쪽 위로 뻗어 첫 레이블의 앞부분이 그림 밖으로 나가 잘렸다(`2026-05` → `26-05`,
  `Data Retention` → `ention`). 뻗는 만큼(최대 40px) 축 좌우를 비운다.
- **y축**: 축 이름(`yLabel`)을 넣을 수 있다. 비워 두면 축을 그리지 않아 기존 화면과 같다.

### 4.6 조회 기간 (기간 필터)

캔버스에 **기간 필터**를 올리면 그 페이지의 모든 바인딩이 고른 기간으로 좁혀진다. Claim 종합 현황이
첫 사용처로, 13개 컴포넌트 전부가 같은 기간 위에서 집계된다. 기본값은 **최근 3개월**.

```
[기간 필터 클릭] → 주소 ?preset=3m  →  서버가 기간을 확정(lib/period.ts)
                                          ↓ from · to
                                    바인딩 필터 source:'query'  →  WHERE 접수일 BETWEEN ?
                                          ↓
                                    페이지 전체 재조회 후 렌더
```

- **주소에 남는다**(`?preset=3m` 또는 `?from=…&to=…`). 링크를 그대로 공유하면 같은 화면이 열린다.
- **기본 기간은 코드가 아니라 설계에 있다.** 컴포넌트 속성(`defaultPreset`)이라 빌더에서 바꾸면 그대로 따라간다.
- **값이 없으면 조건을 걸지 않는다.** 빈 값을 그대로 바인딩하면 아무 행도 안 맞아 빈 화면이 된다 —
  "제한 없음"이라는 의도에 맞게 조건 자체를 뺀다.
- **DATETIME 컬럼의 상한은 그날 끝으로** 올린다(`2026-08-19` → `2026-08-19T23:59:59.999Z`).
  안 그러면 마지막 날 하루가 통째로 빠진다.
- **지금 적용된 기간은 서버가 계산해 내려준다.** 클라이언트가 다시 계산하면 자정 근처에 서버와 어긋난다.

### 4.6.1 좁은 창 대응

운영 화면은 관리자가 12칼럼 캔버스에 찍은 좌표를 그대로 그린다. 창이 좁아지면 세 가지가 겹쳐 무너졌다.

| 무너진 지점 | 원인 | 조치 |
|---|---|---|
| 접힌 글이 **아래 컴포넌트를 덮음** | 줄 높이가 고정(8px)이라 넘친 내용이 밀어내지 못함 | 줄 높이를 `minmax(줄높이, auto)`로 |
| 3칸 지표 타일이 320px 창에서 80px | 폭과 무관하게 12칼럼 유지 | 본문 폭 600px 이하에서 **한 줄에 하나씩** 쌓고, 설계 높이를 최소 높이로 되살림 |
| 카드가 통째로 잘려 나감 | 트랙 `1fr`(= `minmax(auto, 1fr)`)을 가장 넓은 컴포넌트의 최소 내용 폭(표 9칼럼 884px)이 밀어 넓힘 | 트랙 `minmax(0, 1fr)` + 칸 `min-w-0` |

- 기준은 **뷰포트가 아니라 본문이 실제로 쓸 수 있는 폭**(컨테이너 질의)이다 — 같은 창 크기라도
  사이드바가 열려 있으면 본문은 260px 남짓 좁아진다.
- 한 줄씩 쌓일 때는 DOM 순서가 곧 화면 순서라, 최상위 컴포넌트를 `order`가 아니라 **보이는
  순서**(행→열)로 그린다. `order`가 배치와 어긋난 페이지(fa-assign·tips)에서 실제로 달랐다.
- 셸도 함께: 헤더는 제목이 먼저 줄고 오른쪽 도구는 줄지 않으며(좁으면 단계 이름을 접고
  번호 ①②③④만 남긴다), `SidebarInset`에 `min-w-0`을 줘 본문이 사이드바를 밀지 못하게 했다.

### 4.6.2 관리자 화면의 좁은 창 — 칸을 탭으로

빌더 네 칸의 최소 폭 합계는 1,140px이다. 그 아래에서는 어느 칸도 제 몫을 못 해 트리·팔레트·캔버스가
전부 내부 스크롤로 버텼다. **본문 폭 1024px 아래에서는 칸을 탭으로 접는다.**

| 화면 | 넓을 때 | 좁을 때 |
|---|---|---|
| `/admin/builder` | 페이지·팔레트·캔버스·속성 4분할 | 같은 넷을 탭으로 한 번에 하나씩 |
| `/admin/data` | 엔티티 목록 + 상세 2분할 | 고르기 전에는 목록만, 고른 뒤에는 상세만(＜ 돌아가기) |

- **팔레트→캔버스 드래그는 두 칸이 동시에 보여야 성립한다.** 탭 모드에서는 불가능하므로 팔레트
  항목에 ＋ 버튼을 뒀다 — 현재 페이지 **맨 아래**에 붙이고 캔버스 탭으로 넘어간다. 위치 조정은
  캔버스 안에서 끌어 옮기면 되고, 그건 한 칸 안이라 좁은 화면에서도 그대로 된다.
  넓은 화면에서는 마우스를 올렸을 때만 보여 드래그 UX를 방해하지 않는다.
- 페이지를 고르면 캔버스 탭으로 옮겨 준다 — 고른 결과가 다른 탭에 있으면 아무 일도 안 한 것처럼 보인다.

### 4.6.3 사이드바 접기 — 버튼 자리와 글자 전환

- **접기 버튼이 사이드바 헤더 안**에 있다. 예전에는 그 자리에 아무 동작 없는 위아래 화살표(장식)가
  있었고 접기 버튼은 본문 헤더에 따로 떨어져 있었다. 접힌 상태에서는 자리가 없어 감추고, 그때만
  본문 헤더의 버튼이 나타난다 — 둘 다 보이면 같은 일을 하는 버튼이 두 개가 된다.
- **글자는 크로스페이드**한다. 폭이 16rem ↔ 3rem으로 200ms 동안 변하는 내내 글자가 남아 점점
  잘렸다("Claim 종합 현황" → "Claim 종…" → "Cl…"). 이제 접을 때는 기다리지 않고 먼저 사라지고,
  펼칠 때는 폭이 거의 다 늘어난 뒤 떠오른다. `max-width`도 함께 줄여 접힌 상태에서 자리를
  차지하지 않는다. 움직임 최소화를 설정한 사용자에게는 전환 없이 즉시 바뀐다.

### 4.7 추이 차트를 원본 데이터에서 파생

월별·주별 접수 추이와 평균 TAT 추이는 미리 집계해 둔 표(`claim_trend`, 최근 12개월/12주)를 보고 있어서
기간을 바꿔도 따라오지 못했다. `group` 바인딩에 **날짜 묶음**(월/주/연)을 더해 원본 `claims`에서 바로 만든다.

- 어떤 기간을 골라도 그 기간의 추이가 나온다(2023-01부터 전부).
- 시점 수가 상한에 걸리면 **오래된 쪽이 아니라 최근 쪽**을 남긴다 — 그냥 잘라내면 "최근 추이"를 보러 온
  화면이 과거만 보여준다.
- `claim_trend` 표는 이제 아무 화면도 참조하지 않는다(검증 W-DATA-011로 표시됨). 데이터는 그대로 두었다.

---

## 5. 성능 현황 (실측)

데이터를 실사용 규모(약 2만 2천 행)로 채운 뒤 측정.

| 항목 | 조치 전 | 조치 후 |
|---|---|---|
| 목록 조회(claims 5,000행) | 1.18ms · 전체 스캔 + 임시 정렬 | **0.30ms** · 인덱스 스캔 |
| 항목별 집계(GROUP BY) | 0.86ms · 스캔 + 임시 그룹핑 | **0.26ms** · 커버링 인덱스 |
| 게시판 검색 API | 54ms | **15.8ms** (DB 시간 0.3ms) |
| 게시판 검색 확장성(10만 건) | LIKE 8.3ms | **FTS 1.6ms** |
| 동시 10명 `/home` | 292ms | **123ms** (초당 81건) |
| 동시 30명 `/home` | 559ms | **266ms** (초당 113건) |
| 동시 60명 `/home` | — | **373ms** (초당 147건), 오류 0 |
| 페이지 HTML | 184KB | **125KB** (인라인 테마 CSS 제거 + 차트 원시 행 제거) |

---

## 6. 품질 장치

| 명령 | 하는 일 |
|---|---|
| `pnpm typecheck` / `pnpm lint` | 타입·정적 검사 |
| `pnpm test` | 단위 테스트 **282건** |
| `pnpm test:e2e` | Playwright **10건**(테스트 전용 DB로 격리) |
| `pnpm audit:catalog` | 카탈로그 89종을 5가지 입력(예전 노드·기본값·바인딩 null·빈 결과·집계 0)으로 서버 렌더 |
| `pnpm audit:ui` | 실제 화면 17개 × **6해상도**(1600·1280·1024·768·375·320)에서 가로 스크롤·내부 가로 밀림·그리드 칸 넘침·컨테이너 밖 잘림·말줄임 없는 잘림·행 정렬 어긋남·형제 겹침 탐지 |
| 검증 51규칙 | 배포 전 설계 오류 차단(오류 0이어야 배포) |

`audit:catalog`는 "카탈로그에 속성을 추가했더니 기존 노드가 전부 렌더 오류로 떨어진" 사고를,
`audit:ui`는 배치 결함을 배포 전에 잡기 위해 만들었다. 두 도구 모두 일부러 넣은 결함을 잡아내는지 자체 검증을 마쳤다.

---

## 7. 운영 명령

```bash
pnpm dev                 # 개발 서버 (별도 빌드 폴더 .next-dev 사용)
pnpm build && pnpm start # 로컬 운영 모드
pnpm db:backup           # 두 DB 백업(30일 보관)
pnpm db:optimize         # 배포된 스펙이 쓰는 컬럼에 인덱스 생성 + ANALYZE
pnpm audit:catalog       # 컴포넌트 전수 렌더 점검
pnpm audit:ui            # UI 배치 점검(기본 http://localhost:3100)
```

```powershell
# 무중단 재배포(빌드 → 프로세스 전환 → 헬스체크 → 실패 시 롤백 → 인덱스 정리)
powershell -NoProfile -ExecutionPolicy Bypass -File F:\Claude\WebApp_V1\deploy\redeploy.ps1

# 자동 기동 상태 확인
Get-ScheduledTaskInfo -TaskName WebApp_V1-autohost
Get-Content F:\Claude\WebApp_V1\data\logs\autostart.log -Tail 20
```

> **배포 규칙**: 빌드·배포 전에는 **항상 먼저 git commit + push**한다. 배포 후 문제가 보이면 직전 커밋으로 되돌려 재배포한다.

---

## 8. 해결한 문제들 (증상 → 원인 → 조치)

| 증상 | 원인 | 조치 |
|---|---|---|
| 재부팅 후 사이트가 안 뜸 | Run 키 트리거 미발화 + 비대화형에서 `%APPDATA%\npm`이 비어 보임 | 작업 스케줄러 + F: 드라이브 pm2 사본, 전 과정 로깅 |
| 감시가 10분마다 서비스 재시작 | PowerShell 5.1 `ConvertFrom-Json`이 대소문자 중복 키(`username`/`USERNAME`)에서 실패 → 상태 조회가 늘 빈 값 | 상태 파싱을 node로 이관 |
| 운영 차트 6개가 "렌더링 오류" | 새 속성(`yLabel`) 추가 후 기존 노드에 그 값이 없어 `undefined.trim()` | 런타임·캔버스가 카탈로그 기본값을 먼저 깔도록 수정 + `audit:catalog` 상시화 |
| 서체가 안 먹음 | `--font-sans: var(--font-sans)` 순환 + 폰트 변수가 `<body>`에만 있음 | 변수를 직접 가리키게 하고 `<html>`로 이동 |
| 페이지 보기에서 드래그가 안 따라옴 | 매 렌더마다 저장 좌표로 위치를 덮어씀 | 그리는 좌표를 `nodes` 하나로 통일, 보기 전환 때만 반영 |
| 차트 수치가 틀림 | 원시 행 200건 표본으로 집계 | `group` 바인딩(GROUP BY) 추가 |
| 배포마다 서비스 중단 | 서비스 중인 `.next`를 비우며 빌드 | 블루/그린 빌드 폴더 + 프로세스 전환 |
| 한글 주석이 깨짐 | PowerShell 5.1이 UTF-8을 ANSI로 읽고 BOM을 붙임 | JSON 손질을 `scripts/dist-dir.mjs`(Node)로 이관 |
| 검색이 느려짐(색인 적용 후) | Prisma raw가 FTS 질의를 매번 다시 준비 | 검색만 읽기 전용 드라이버 연결 |
| 항목별 집계 막대가 전부 1 | 개수 집계의 값 컬럼은 대응 필드가 없어 `fieldId`가 null인데, 차트가 그걸로 "관리자가 고른 컬럼"을 판별해 값 컬럼을 버림 | 암묵 컬럼 표시를 명시 플래그로 분리, 계열 변환 규칙을 `lib/chart-series.ts` 한 곳으로 |
| `audit:ui`가 운영 화면을 한 장도 안 봄 | 화면 목록을 인증이 필요한 `/api/admin/pages`에서 받는데 운영 서버는 세션 쿠키가 secure라 http로 로그인 불가 → 목록이 조용히 빔 | 공개 엔드포인트 `/api/runtime/spec`에서 받고, 관리자 세션이 없으면 건너뛴다고 알림 |
| 좁은 창에서 카드가 겹치고 잘림 | 고정 줄 높이 + 폭 무관 12칼럼 + `1fr` 트랙 최소 크기(§4.6.1) | 줄 높이 `minmax(…, auto)`, 600px 이하 한 줄 쌓기, 트랙 `minmax(0, 1fr)` |
| 그 잘림을 `audit:ui`가 못 잡음 | 문서 전체 폭만 봤는데, 본문은 `overflow-y: auto` 영역 **안에서** 옆으로 밀려 문서 폭은 그대로였다 | 내부 스크롤·그리드 칸 넘침 규칙 2개 추가 + 좁은 폭 뷰포트 4종 상시 점검 |

---

## 9. 알려진 한계 · 다음 후보

- **자동 기동은 로그인 후에 동작한다**(작업 스케줄러 "사용자 로그온 시"). 로그인 없이 부팅부터 띄우려면 관리자 권한으로 Windows 서비스 등록이 필요하다.
- **게시판 2글자 검색은 여전히 LIKE 전체 스캔**이다(trigram 한계). 글이 수만 건이 되고 짧은 검색어가 잦다면 별도 색인이 필요하다.
- **검증 경고 15건**(홈에서 NAVIGATES로 도달 불가)은 이 앱이 사이드바로 이동하는 구조라 규칙과 어긋나는 경우다. 관계를 억지로 만들지 않고 남겨 뒀다.
- **채팅 지연 0.7초**(워커 간 전파 주기). 더 즉각적인 전파가 필요하면 외부 pub/sub 도입을 검토한다.
- **기간 필터는 페이지당 하나**를 전제로 한다(주소 파라미터 이름이 `from`/`to`로 고정). 한 화면에서 두 구간을
  따로 걸어야 하면 파라미터 이름에 접두사를 붙이는 확장이 필요하다.
- **날짜 묶음 집계는 인덱스를 타지 않는다**(`strftime(...)` 식 위의 GROUP BY). claims 5,000행에서는 1ms 남짓이라
  문제가 없지만, 행이 수십만이 되면 생성 컬럼 + 인덱스를 검토한다. 기간 조건 쪽은 인덱스를 그대로 탄다.
- **`claim_trend` 표가 남아 있다.** 추이를 원본에서 파생시키면서 참조가 사라졌지만, 엔티티 삭제는 파괴적
  스키마 변경이라 그대로 두었다.
- **좁은 창의 빌더는 드래그 배치를 쓸 수 없다**(§4.6.2). 팔레트의 ＋ 버튼으로 맨 아래에 추가한 뒤
  캔버스 안에서 옮기는 방식이다. 정밀한 배치 작업은 여전히 넓은 화면이 훨씬 편하다.
- **캔버스는 좁은 창에서도 12칼럼 데스크톱 배치를 그대로 보여준다.** 설계 결과를 있는 그대로 보여주는
  WYSIWYG 면이라 의도된 동작이고, 운영 화면(`/home`)의 좁은 폭 쌓기와는 다르다.
- 동시 접속이 지금보다 크게 늘면 워커 수 상향(현재 4/24코어)이 첫 번째 카드다.
